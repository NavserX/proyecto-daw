# Proyecto Final DAW
## Instrucciones para el despliegue
1. Ir a carpeta `/backend` y ejecutar: `docker compose -f docker-compose.dev.yml up -d`
2. Ir a carpeta `/frontend` y ejecutar: `docker compose up -d`
3. Abrir `http://localhost` en el navegador.
